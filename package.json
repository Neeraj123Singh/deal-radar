{
  "name": "deal-radar",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time AI co-pilot for sales teams — Overpath take-home",
  "scripts": {
    "dev": "concurrently -n backend,frontend,generator -c blue,green,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix generator\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:generator": "npm run dev --prefix generator",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "docker:up": "docker compose up --build",
    "docker:up:generator": "docker compose --profile with-generator up --build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
