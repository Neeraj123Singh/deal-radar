# ── Required for AI health scoring (optional — rule-based fallback works without it)
OPENAI_API_KEY=sk-your-openai-key-here

# ── Backend (defaults work with docker-compose)
PORT=3001
DATABASE_URL=postgresql://dealradar:dealradar@localhost:15432/dealradar
REDIS_URL=redis://localhost:6379
CORS_ORIGIN=http://localhost:3000

# ── Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001

# ── Mock event generator
WEBHOOK_URL=http://localhost:3001/webhook/events
INTERVAL_MS=2000
